{
  "name": "@shadow-sniper/contract",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "compact compile src/shadow_sniper.compact managed/",
    "test": "node --test src/test/*.test.ts",
    "clean": "rm -rf managed dist .compact-cache"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "typescript": "^5.7.2"
  }
}
